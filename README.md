# RIFTLANE

A TypeScript browser FPS monorepo built with a shared library, Node server, and Vite client. The project combines WebSocket networking, Three.js rendering, and a modular architecture across three npm workspaces.

## Getting Started

```bash
npm install
npm run dev:server
npm run dev:client
npm test
```
